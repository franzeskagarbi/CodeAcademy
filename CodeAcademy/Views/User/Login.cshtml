@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model CodeAcademy.Models.LoginViewModel
@{
    ViewBag.Title = "Log In";
}
<style>
    .container{
        width:800px;
    }
</style>

<body style="background-color: #ccffee;">
    <div class="container">
        <br />
        <h1 class="text-center" style="font-size: 2rem;">Log in to CodeAcademy</h1>
        <br />
        <div class="row justify-content-center">
            <div class="col-md-6">
                <form asp-action="Login">
                    @if (TempData["RoleError"] != null)
                    {
                        <div class="alert alert-danger">
                            @TempData["RoleError"]
                        </div>
                    }

                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group">
                        <label asp-for="Username" class="control-label"></label>
                        <input asp-for="Username" class="form-control" style="background-color: white; border: 1px solid black; border-radius: 0; width: 100%;" />
                        <span asp-validation-for="Username" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Password" class="control-label"></label>
                        <input asp-for="Password" class="form-control" type="password" style="background-color: white; border: 1px solid black; border-radius: 0; width: 100%;" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <br />
                    <div class="form-group">
                        <input type="submit" value="Log In" class="btn btn-primary" />
                    </div>
                    <br />
                    <div class="text-center">
                        Don't have an account? <a asp-action="Create">Sign up here.</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</body>



@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
